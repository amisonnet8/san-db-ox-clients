# CLAUDE.md

このファイルは san-db-ox-clients プロジェクトで作業する際、Claude Code が
常に踏まえるべき前提を示す。

## san-db-ox-clients とは

[SanDBox](https://github.com/amisonnet8/san-db-ox)（DBエンジンとデータ領域を
1つの実行ファイル内に保持する、環境構築不要のポータブルな単一バイナリ
RDBMS）を、各プログラミング言語から子プロセスとして起動し、stdio プロトコル
（本体仕様書 §7）で対話するための、各言語向けの薄いラッパー群を提供する
リポジトリ。

**ドライバは利便性のためのものであって、利用の前提条件ではない。** stdio
プロトコルは行区切りJSON（JSON Lines）であり、サブプロセスとパイプを扱える
処理系であれば専用ライブラリなしで直接繋がる。ドライバはその上に乗る薄い
ラッパーとして、型付けされた API・値の表現の変換・トランスポートの選択
（`.claude/rules/architecture.md`）を提供する。

## 本体との関係

**プロトコルの正典は本体（san-db-ox）の仕様書であり、このリポジトリでは
プロトコルの設計判断を行わない。** 追従しているタグ・op一覧・エラーコード等は
`.claude/rules/protocol.md` に固定してある。本体側でプロトコルに関わる変更が
あった場合は、まず `.claude/rules/protocol.md` を更新してから実装へ反映する。

仕様に不備や矛盾を見つけた場合は、このリポジトリ側で独自に回避せず、まず
本体の仕様書を直すことを提案する。本体の仕様書が更新されたことを確認して
から、こちらの実装へ反映する。この順序を逆にしない。

## 参照すべきファイル

作業を始める前に、以下を確認すること。

- **`.claude/rules/protocol.md`** — 追従している本体のタグ・プロトコル
  バージョン、op 一覧、ドライバが必ず満たす契約（フラッシュ・stderr・EOF・
  終了処理等）、値のJSON表現。**最初に読むファイル。**
- **`.claude/rules/architecture.md`** — 全言語のドライバが共有する構造
  （コーデック層とトランスポート層の分離、直結／ソケットの2トランス
  ポート、起動コマンドを差し替え可能にする設計）。
- **`.claude/rules/connectivity.md`** — 接続方法（直結・SSH・socat）と
  そのセキュリティ設定（forced command、TLS、接続元IP制限）。本体仕様書
  §8 が「本書の範囲外、`san-db-ox-clients` のドキュメントで扱う」と
  明記した領域。
- **`.claude/rules/naming.md`** — 言語間で op 名の語幹を揃える規則、製品名
  の3段階表記、各言語のモジュール／パッケージ名。
- **`.claude/rules/directory-structure.md`** — ディレクトリ構成
  （言語ごとに1ディレクトリ、共有物の置き場所）。
- **`.claude/rules/testing.md`** — テスト方針（本体バイナリの入手方法、
  ハング検出、conformance スイート、shellcheck/trivy の実施方法）。
- **`.claude/rules/distribution.md`** — 配布方法（各言語標準の配布方法の
  みを使い、共通の配布方法は設けない）。
- **`PLAN.md`** — 実装計画・進捗管理。セッションをまたぐたびに「現在地」を
  確認・更新すること。

## 開発の進め方

- 現在の開発フェーズは `PLAN.md` の「現在地」を確認すること。フェーズが
  進んだら、そちらを更新すること。
- 実装後は `.claude/rules/testing.md` に従い、動作確認を行うこと。判断に
  迷ったら実行する側に倒すこと。
- **設計判断を変える場合、それがプロトコルに関わるものであれば、まず本体
  （san-db-ox）の仕様書を更新する提案をすること。** このリポジトリの
  `.claude/rules/` だけを直して済ませない。

## ドキュメントの書き方

本体 CLAUDE.md の方針を継承する。**提供しない機能については、事実だけを
書き、代替手段を書かない。** 本体が「ドライバに `iterate` 系のヘルパーを
標準で持たせない」と明記している判断（`.claude/rules/protocol.md` 参照）は、
この方針の適用例そのものである。

**例外が1つある——接続方法とセキュリティ設定は、この制限の対象外として
詳しく書く。** 本体の「代替手段を書かない」は「提供しない機能の回避策を
書くな」という意味であり、本体仕様書 §7 が「守らないと動かないものは契約
だから仕様書に書く」と例外を設けているのと同じ構造で、**守らないと危険な
設定は運用上の契約**として書く対象になる。本体仕様書 §8 がセキュリティ
設定を明示的にこのリポジトリへ委譲していることも根拠になる（詳細は
`.claude/rules/connectivity.md`）。

## ドキュメントの言語

本体と同一の方針を採る。

- **`.claude/rules/`・`PLAN.md`・`CLAUDE.md` は日本語のみ。** 英語版は
  作らない。
- **`docs/` 配下は日本語版を先に作り、実装完了後に英語版を整える。**
  日本語版のファイル名には `_ja` サフィックスを付ける。
- **コード・コメント・APIが返す文字列は英語。** 対話は日本語で行うが、
  これらは英語で書くこと。

## 権限・自動化について

`.claude/settings.json`（配置済み）により、`git push`/`pull`/`fetch`/
`clone`、`curl`/`wget`、パッケージインストール系コマンド（`*install*`,
`go get`）、`sudo` は実行前に確認を求める設定になっている。確認を求め
られた場合、無理に実行しようとせず、指示を仰ぐこと。

ビルドの自動フック（`PostToolUse`）は現時点では設定していない。Go ドライバ
（フェーズ③）に着手し `Makefile` にビルドターゲットが入った時点で、本体と
同じ運用（`.go`/`go.mod`/`go.sum` 編集時の自動ビルド確認）を提案すること。

## ルール・スキルの提案

作業を進める中で、以下に気づいたら、都度こちらから提案すること（提案する
だけで、勝手に作成・適用はしない。判断はこちらが行う）。

- **新しいルールにした方がよさそうな知見**: 同じ種類の判断や落とし穴に
  複数回遭遇した、既存の `.claude/rules/` のどれにも当てはまらない、と
  いった場合。
- **Skill化した方が効率的そうな作業**: パターン化できそうな一連の作業に
  気づいた場合。パターンが実際に繰り返されてから提案すること。

提案は気づいたタイミングで随時行ってよく、まとめて報告するために貯めて
おく必要はない。
